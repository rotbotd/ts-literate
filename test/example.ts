/// # Example Literate TypeScript
///
/// This is a simple example of literate programming with TypeScript.
/// We use /// comments for prose and regular code for... code.

function greet(name: string): string {
  return `Hello, ${name}!`;
}

/// Now let's use the function:

const message = greet("world");
console.log(message);

/// That's all folks!
